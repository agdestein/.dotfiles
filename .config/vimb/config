# Home
# set home-page=http://github.com

# Downloads
set download-path=~/Downloads

# UI
set stylesheet=off
set smooth-scrolling=on
set incsearch=true
set monospace-font=JuliaMono
# set monospace-font-size=13
set dark-mode=on

# # SSL
# set strict-ssl=false

# # JS
# set javascript-can-access-clipboard=off
# set javascript-can-open-windows-automatically=off

# Editing
set editor-command=smallacritty -e nvim %s

# Zoom in everything by default
# aug zoom_all
#   au LoadCommited * normal zz|8zI
# aug end

nnoremap <C-t> :set stylesheet!<CR><Esc>
nnoremap <F11> :set fullscreen!<CR><Esc>
nnoremap <C-l> :source ~/.config/vimb/config<CR>

# If enabled the inputbox will be hidden whenever it contains no text.
set input-autohide=true

# Enable or disable the spell checking feature.
set spell-checking=true

# Set comma separated list of spell checking languages to be used for
# spell checking.
set spell-checking-languages=en,fr,ru,no

# Enable or disable support for WebGL on pages.
set webgl=true

# While typing a search command, show where the pattern typed so far matches.
set incsearch=true

# The font family to use as the default for content that does not specify a
# font.
# set default-font=DejaVu Sans
set default-font=Noto Sans
# set default-font=Latin Modern Roman

# The font family used as the default for content using monospace font.
set monospace-font=JuliaMono

# The font family used as the default for content using sans-serif font.
# set sans-serif-font=DejaVu Sans
set sans-serif-font=Noto Sans
# set sans-serif-font=Latin Modern Sans
# set sans-serif-font=Cantarell

# The font family used as the default for content using serif font.
# set serif-font=DejaVu Serif
set serif-font=Noto Serif
# set serif-font=Latin Modern Roman

# The default font size used to display text.
set font-size=16

# Default font size for the monospace font.
set monospace-font-size=13

# Default Full-Content zoom level in percent. Default is 100.
set default-zoom=100

# set hint-keys=asdfg;lkjh

# Map page zoom in normal mode to keys commonly used across applications
# + (zoom in), - (zoom out), = (zoom reset)
nmap + zI
nmap - zO
nmap = zz

source ~/.config/vimb/cfg/shortcuts

## BEGIN THEME
# Gruvbox
set completion-css=color:#ebdbb2; background-color:#282828; font:12pt JuliaMono;
set completion-hover-css=color:#ebdbb2; background-color:#282828; font:12pt JuliaMono;
set completion-selected-css=color:#ebdbb2; background-color:#928374; font:12pt JuliaMono;
set input-css=color:#ebdbb2; background-color:#282828; font:12pt JuliaMono;
set input-error-css=color:#cc241d; background-color:#282828; font:12pt JuliaMono;
set status-css=color:#d79921; background-color:#282828; font:12pt JuliaMono;
set status-ssl-css=color:#b8bb26; background-color:#282828; font:12pt JuliaMono;
set status-ssl-invalid-css=color:#b16286; background-color:#282828; font:12pt JuliaMono;
## END THEME
